// Next.js App Router uses file-based routing. The hook expects barrel re-exports;
// these are unused by the framework itself.
export { default as page } from "./page";
export { default as layout } from "./layout";
